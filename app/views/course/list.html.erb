
<h1 class="mrg_head">List of all tests</h1>

<div class="wrap">
<table width="60%">
	<tr>
		<th>Title</th>
		<th>Subject</th>
		<th>Author</th>
		<th>Questions</th>
	</tr>

	<% @courses.each do |course| %>
		<tr>
			<td><%= course.title %></td>
			<td><%= course.subject.name %></td>
			<td><%= course.author.name %></td>
			<td align="center"><%= course.questions %></td>
		</tr>
	<% end %>

</table>

<br />

<%= link_to("Add new test", :action => 'new') %>

</div>