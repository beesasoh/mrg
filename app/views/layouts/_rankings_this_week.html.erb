<h3>Rankings this week</h3>
<div class="side-bar-item">
    <div class="side-bar-section">
    <ul>
        <% ranking_arr = Game.rankings_this_week(10).keys %>
        <% ranking_arr.each do |user_id| %>
            <% user = User.find_by_id(user_id) %>
            <li class="ur" 
                dt-img  = "http://graph.facebook.com/<%= user.uid %>/picture?width=100&height=100"
                dt-name = "<%= user.name %>" 
                dt-link  = "/profile/<%= user.id %>"
                dt-rank = "<%= user.rank %>" >
                <div class = "left">
                    <span class="fb-img">
                        <%= image_tag("http://graph.facebook.com/#{user.uid}/picture?width=100&height=100") %>
                    </span>
                </div>
                <div class = "right">
                    <span class="fb-name"><%= user.name %></span><br />
                    <strong><%= user.points_this_week %></strong>
                </div>
                <div class="clear-fix"></div>
            </li>
        <% end %>
    </ul>
    </div>
</div>